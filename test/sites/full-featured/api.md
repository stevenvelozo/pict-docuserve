# API Reference

## `create(options)`

Create a new instance.

### Parameters

- `options.name` — The instance name
- `options.config` — Configuration object

### Returns

An initialized instance ready for use.

## `instance.run()`

Start the instance.

## `instance.stop()`

Stop the instance gracefully.
