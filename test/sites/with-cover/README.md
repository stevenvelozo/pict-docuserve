# Cover + TopBar Test

This site has `cover.md` and `_topbar.md` but no `_sidebar.md`, no catalog and no keyword index.

The splash page should be driven entirely by `cover.md` and the top bar by `_topbar.md`.

The sidebar should auto-discover from this README since no `_sidebar.md` is present.
